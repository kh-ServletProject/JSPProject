<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<!-- 중복하는ID 조회 -->
	<entry key="findId">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE MEMBER_ID = ?
	</entry>

	<!-- 회원가입 -->
	<entry key="signUp">
		INSERT INTO MEMBER
		VALUES(SEQ_MEMBER.NEXTVAL, ?, ?, ?)
	</entry>

	<!-- 로그인 회원 조회 -->
	<entry key="loginMember">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_PW, MEMBER_NAME
		FROM MEMBER
		WHERE MEMBER_ID = ? AND MEMBER_PW = ?
	</entry>

	<!-- 메모 리스트 조회 -->
	<entry key="memberMemoList">
		SELECT MEMO_NO, MEMO_TITLE, MEMO_CONTENT,
		TO_CHAR(WRITE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS WRITE_DATE,
		TO_CHAR(UPDATE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS UPDATE_DATE,
		MEMBER_NO, DELETED
		FROM MEMO
		WHERE MEMBER_NO = ?
	</entry>

	<!-- 메모 수정 -->
	<entry key="memoUpdate">
		UPDATE MEMO
		SET MEMO_TITLE = ?,
		MEMO_CONTENT =?,
		UPDATE_DATE = SYSDATE
		WHERE MEMO_NO = ?
	</entry>

	<!-- 디테일 목록 조회 -->
	<entry key="memoDetail">
		SELECT MEMO_NO, MEMO_TITLE, MEMO_CONTENT, WRITE_DATE, UPDATE_DATE, MEMBER_NO
		FROM MEMO
		WHERE MEMO_NO = ?
	</entry>
	
	<!-- 메모 삭제 -->
	<entry key="memoDelete">
		DELETE FROM MEMO WHERE MEMO_NO = ?
	</entry>

	<!--
	 휴지통 메모 조회
	<entry key="memoBin">
		SELECT MEMO_NO, MEMO_TITLE, MEMO_CONTENT,
		TO_CHAR(WRITE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS WRITE_DATE,
		TO_CHAR(UPDATE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS UPDATE_DATE, MEMBER_NO
		FROM MEMO
		WHERE MEMBER_NO = ?
		AND DELETED = 'y'
	</entry>
	-->

	<!-- 메모 휴지통으로 이동 -->
	<entry key="goBin">
		UPDATE MEMO
		SET DELETED = 'y'
		WHERE MEMO_NO = ?
	</entry>

	<!-- 메모 추가 -->
	<entry key="addMemo">
		INSERT INTO MEMO
		VALUES (SEQ_MEMO.NEXTVAL , ?, ?, DEFAULT, DEFAULT, ?, DEFAULT)
	</entry>

	<!-- 메모 복구 -->
	<entry key="memoRollback">
		UPDATE MEMO
		SET DELETED = 'n'
		WHERE MEMO_NO = ?
	</entry>

</properties>