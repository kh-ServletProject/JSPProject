<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
  
	<!--  sql 작성  -->
	
	<!-- 로그인 회원 조회 -->
	<entry key="loginMember">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_PW, MEMBER_NAME
		FROM MEMBER
		WHERE MEMBER_ID = ? AND MEMBER_PW = ?
	</entry>

	<!-- 메모 리스트 조회 -->
	<entry key="memberMemoList">
		SELECT MEMO_NO, MEMO_TITLE, MEMO_CONTENT,
		TO_CHAR(WRITE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS WRITE_DATE,
		TO_CHAR(UPDATE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS UPDATE_DATE, MEMBER_NO
		FROM MEMO
		WHERE MEMBER_NO = ?
	</entry>

</properties>
